apiVersion: v1
kind: Service
metadata:
  name: async-compare-service
spec:
  selector:
    app: async-compare
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 80  
  type: NodePort
